services:
  db:
    image: postgres:17
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: cwie
      POSTGRES_PASSWORD: cwie
      POSTGRES_DB: cwie_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
