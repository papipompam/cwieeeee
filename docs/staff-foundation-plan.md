# แผนพัฒนาระบบส่วนเจ้าหน้าที่ระยะแรก

## เป้าหมาย

สร้างข้อมูลตั้งต้นที่เจ้าหน้าที่ต้องใช้ก่อนเริ่มกระบวนการคำร้องและการนิเทศสหกิจศึกษา ได้แก่ รอบสหกิจ นักศึกษา อาจารย์ และสถานประกอบการ โดยทำแต่ละส่วนให้ใช้งานจริงครบตั้งแต่หน้าจอ API จนถึงฐานข้อมูล

> สถานะเอกสาร: ส่วน `Student`, `Teacher` และ `Staff` ในเอกสารนี้เป็นแผนก่อนตัดสินใจ
> รวมข้อมูลบุคคลและบัญชีเป็น `User` ตารางเดียว ห้ามใช้หัวข้อเหล่านั้นเป็นข้อกำหนด
> implementation ใหม่ ให้ทำตาม [user-unification-plan.md](user-unification-plan.md) ก่อน
> แล้วใช้ [student-application-plan.md](student-application-plan.md) สำหรับ feature นักศึกษา
> เอกสารใหม่มีผลเหนือกว่าเมื่อเนื้อหาขัดกัน

แผนนี้ยังไม่รวมระบบเข้าสู่ระบบ การยื่นคำร้อง การเลือกสถานประกอบการ การจัดอาจารย์นิเทศ เอกสารราชการ และผลการนิเทศ

## วิธีใช้แผนนี้กับ agent

แผนนี้แบ่งเป็น vertical slice ที่ใช้งานได้จริง: ฐานข้อมูล, API และ UI ของ **หนึ่ง** feature
ต้องเสร็จในงานเดียวกัน แต่ agent ทำได้เพียงระยะที่ผู้ใช้ระบุใน request นั้นเท่านั้น

- ก่อนเริ่มอ่าน `AGENTS.md`, ระยะที่ได้รับมอบหมาย และโค้ดที่เกี่ยวข้อง; ตรวจ `git status` แล้วรักษาการแก้ไขเดิมที่ไม่เกี่ยวข้อง
- สำหรับระยะที่ได้รับมอบหมาย ให้สร้างหรือแก้ Prisma model และ migration, Nitro API ภายใต้ `server/api/`, แล้วต่อหน้าจอให้เรียก API จริง แทนข้อมูลตัวอย่าง
- ทำเฉพาะ endpoint ที่หน้าจอของระยะนั้นใช้จริง: รายการ, เพิ่ม, แก้ไข และลบหรือเปลี่ยนสถานะตามกฎข้อมูลของ feature นั้น
- Validation, unique constraint, การปฏิเสธการลบ และการล็อกข้อมูลเป็นหน้าที่ของ API และฐานข้อมูล ไม่ใช่ UI อย่างเดียว
- ห้ามสร้าง model, migration, endpoint, route หรือ UI ของระยะถัดไป และห้ามสร้างตารางเชื่อม/abstraction/general CRUD layer ไว้ล่วงหน้า
- ห้าม commit, เพิ่ม dependency, หรือเปลี่ยนวิธีจัดการ authentication โดยไม่ได้รับคำสั่งตรง
- เมื่อจบ ให้รัน checks ของระยะนั้น, ตรวจ final diff และรายงานสิ่งที่เปลี่ยน/สิ่งที่ยังอยู่นอกขอบเขต

## หลักการร่วม

- ใช้ Nuxt 4, Nuxt UI, Prisma และ PostgreSQL ตามโครงสร้างปัจจุบัน
- ใช้ semantic colors และ UI primitives ที่กำหนดไว้ใน `AGENTS.md` และ `app/app.config.ts`
- แบบฟอร์มต้องแสดง validation ใกล้ช่องข้อมูล และใช้ toast แจ้งผลระดับ action
- รายการทุกหน้ารองรับสถานะว่าง กำลังโหลด และเกิดข้อผิดพลาด; เพิ่มการค้นหาหรือตัวกรองเฉพาะข้อมูลที่มีและช่วยงานเจ้าหน้าที่จริง
- ยืนยันก่อนลบด้วย `UIConfirmModal`
- ลบได้เมื่อข้อมูลยังไม่ถูกอ้างอิง หากถูกใช้งานแล้วให้ปฏิเสธการลบและแนะนำให้เปลี่ยนสถานะเป็นไม่ใช้งานหรือปิดรอบ
- ยังไม่สร้าง abstraction กลางหรือเพิ่ม dependency จนกว่าจะมีการใช้งานซ้ำจริง

## 1. รอบสหกิจ

### สถานะก่อนมอบหมายงาน

มี `CooperativeCycle` model, migration, Nitro CRUD API และหน้า
`/staff/cooperative-cycles` อยู่แล้วใน working tree. งานที่มอบหมายต่อไปคือ
**ระยะ A-Completion** เพื่อทำให้กฎข้อมูลของรอบสหกิจตรงตามแผนนี้ ไม่ใช่สร้าง
model, migration, endpoint หรือหน้าจอซ้ำ. Agent ต้องตรวจสถานะ migration และ
โค้ดเดิมก่อน แล้วแก้เฉพาะช่องว่างที่ระบุด้านล่าง

### ข้อมูล

- ภาคเรียน เป็นจำนวนเต็ม เช่น `2`
- ปีการศึกษา พ.ศ. เช่น `2569`
- รุ่นนักศึกษา พ.ศ. เช่น `2566`
- วันเปิดรับคำร้อง
- วันปิดรับคำร้อง
- วันเริ่มฝึก
- วันสิ้นสุดฝึก
- สถานะ:
  - เปิดรับคำร้อง
  - ปิดรับคำร้อง
  - กำลังฝึกงาน
  - ปิดรอบ
- หมายเหตุ ไม่บังคับ

ไม่ใช้สถานะ `ร่าง` เพื่อลดขั้นตอน เมื่อสร้างรอบใหม่ให้สถานะเริ่มต้นเป็น `เปิดรับคำร้อง` เจ้าหน้าที่เปลี่ยนสถานะได้ทันทีจากฟอร์มแก้ไขเดิม โดยไม่ต้องมี workflow อนุมัติเพิ่ม

`ปิดรอบ` เป็นสถานะสุดท้าย (terminal): เมื่อบันทึกแล้ว API ต้องปฏิเสธการแก้ไข
รวมถึงการเปลี่ยนสถานะกลับ และการลบ. หากภายหลังต้องเปิดรอบใหม่ ให้ผู้ใช้กำหนด
flow และสิทธิ์ของการเปิดรอบใหม่ก่อน; ห้ามเดา flow นี้ขึ้นเอง

ชื่อที่แสดงให้ประกอบจากข้อมูล เช่น `ภาคเรียนที่ 2/2569 รุ่น 2566` โดยไม่เก็บชื่อซ้ำในฐานข้อมูล

### กฎข้อมูล

- ภาคเรียนเป็นจำนวนเต็ม `1`–`3`; ปีการศึกษาและรุ่นเป็นจำนวนเต็มบวก
- วันที่ต้องเป็นวันปฏิทินจริง ไม่รับวันที่ที่ JavaScript ปรับค่าให้เอง
- ลำดับวันต้องเป็น `วันเปิดรับ ≤ วันปิดรับ ≤ วันเริ่มฝึก ≤ วันสิ้นสุดฝึก`
- ไม่อนุญาตให้มีรอบที่ภาคเรียนและปีการศึกษาซ้ำกัน
- unique constraint ที่ `(term, academicYear)` เป็น source of truth; API ต้องแปลง
  database unique violation เป็น `409 Conflict` ด้วย แม้มีการตรวจซ้ำก่อนบันทึก
- ระยะนี้ยังไม่มีคำร้องหรือรายการฝึกงาน จึงอนุญาตให้ลบรอบที่ยังไม่ `ปิดรอบ` ได้
  เมื่อเริ่ม `Application` ให้เพิ่ม relation และปฏิเสธการลบรอบที่ถูกอ้างอิงในงานนั้น

### หน้าจอ

- `/staff/cooperative-cycles` รายการ ค้นหาจากภาคเรียน ปีการศึกษา หรือรุ่น กรองสถานะ เพิ่ม แก้ไข และลบ
- ตารางใช้ action ที่มองเห็นได้โดยตรงในคอลัมน์ `จัดการ` เช่น แก้ไขและลบ ไม่ซ่อนในเมนู `...`
- รอบที่ `ปิดรอบ` ต้องไม่เสนอ action แก้ไขหรือลบ; แสดงสถานะพร้อมข้อความชัดเจน
- ใช้ modal หรือ slideover สำหรับฟอร์มได้ถ้าข้อมูลยังพอดีในหน้าจอ ไม่สร้างหน้ารายละเอียดจนกว่าจะมีข้อมูลที่ต้องแสดงจริง
- มี workspace route, context header และ navigation ภายในรอบเพื่อให้ผู้ใช้รักษา context ของรอบได้ แต่แต่ละโมดูลจะเพิ่ม data model, API และการจัดการข้อมูลเมื่อเริ่ม feature นั้นจริง

## 2. นักศึกษาและอาจารย์

### นักศึกษา

- รหัสนักศึกษา เป็นข้อความและไม่ซ้ำ
- คำนำหน้า
- ชื่อ
- นามสกุล
- เพศ
- รุ่น พ.ศ. เช่น `2566`
- หมู่เรียน เป็นจำนวนเต็มบวก เช่น `1`, `2`, `4`
- สถานะใช้งาน

รุ่นและหมู่เรียนเก็บในรายการนักศึกษาโดยตรงก่อน ยังไม่สร้างตารางรุ่นหรือห้องเรียน เพราะขณะนี้ไม่มีข้อมูลอื่นของรุ่นและห้องที่ต้องจัดการแยก

### อาจารย์

- รหัสอาจารย์ เป็นข้อความและไม่ซ้ำ
- คำนำหน้า
- ชื่อ
- นามสกุล
- เพศ
- เบอร์มือถือ
- สถานะใช้งาน

### กฎข้อมูล

- คำนำหน้าไม่อนุมานจากเพศ
- เบอร์มือถือเก็บเป็นข้อความเพื่อรักษาเลขศูนย์นำหน้า
- รายการที่เคยถูกอ้างอิงในกระบวนการสหกิจห้ามลบ ให้เปลี่ยนเป็นไม่ใช้งานแทน

### หน้าจอ

- `/staff/students` ค้นหาด้วยรหัสหรือชื่อ และกรองรุ่น หมู่ และสถานะ
- `/staff/teachers` ค้นหาด้วยรหัสหรือชื่อ และกรองสถานะ
- ทำ CRUD นักศึกษาให้เสร็จก่อน แล้วจึงนำรูปแบบหน้ารายการและฟอร์มที่พิสูจน์แล้วไปใช้กับอาจารย์

## 3. สถานประกอบการ

### ข้อมูล

- ชื่อสถานประกอบการ
- ชื่อบุคลากรผู้ติดต่อหลักหนึ่งคน
- เบอร์โทรศัพท์ ไม่บังคับ
- อีเมล ไม่บังคับ
- บ้านเลขที่/อาคาร
- หมู่ ไม่บังคับ
- ซอย ไม่บังคับ
- ถนน ไม่บังคับ
- ตำบล/แขวง
- อำเภอ/เขต
- จังหวัด
- รหัสไปรษณีย์
- ละติจูด ไม่บังคับ
- ลองจิจูด ไม่บังคับ
- หมายเหตุการเดินทาง ไม่บังคับ
- สถานะใช้งาน

ไม่เก็บเลขประจำตัวผู้เสียภาษี และยังไม่สร้างตารางบุคลากรแยก

### ความยืดหยุ่นของชื่อบุคลากร

ชื่อบุคลากรในสถานประกอบการเป็นเพียงค่าเริ่มต้นสำหรับการติดต่อหรือออกเอกสารครั้งใหม่ เมื่อสร้างรายการฝึกงานหรือเอกสารในอนาคต ให้คัดลอกชื่อนี้ไปเก็บในรายการนั้นและเปิดให้เจ้าหน้าที่แก้ชื่อเฉพาะครั้งได้

วิธีนี้ทำให้:

- ฟอร์มสถานประกอบการยังกรอกบุคลากรเพียงคนเดียว
- เปลี่ยนผู้ติดต่อหลักของบริษัทได้ง่าย
- เอกสารเก่ายังคงชื่อบุคคลเดิม ไม่เปลี่ยนตามข้อมูลบริษัท
- ไม่ต้องสร้างระบบจัดการบุคลากรหลายคนก่อนมีความจำเป็น

### ที่อยู่และแผนที่

- เก็บที่อยู่แบบแยกช่องและเก็บพิกัดเป็น `latitude` กับ `longitude`
- จังหวัดเลือกจากรายการ ส่วนภูมิภาคให้คำนวณจากจังหวัดเพื่อป้องกันข้อมูลขัดกัน
- ระยะแรกสามารถกรอกที่อยู่และพิกัดได้โดยยังไม่ผูกผู้ให้บริการแผนที่
- ขั้นเพิ่มแผนที่ต้องรองรับค้นหาสถานที่ คลิกปักหมุด ลากหมุด และแสดงตำแหน่งเดิมเมื่อแก้ไข
- เลือกผู้ให้บริการแผนที่หลังประเมิน API key ค่าใช้จ่าย การค้นหาที่อยู่ภาษาไทย และเงื่อนไขการใช้งาน

### หน้าจอ

- `/staff/companies` ค้นหาด้วยชื่อ กรองจังหวัด ภูมิภาค และสถานะ
- `/staff/companies/new` และ `/staff/companies/[id]` ใช้หน้าฟอร์มเต็ม เพราะข้อมูลที่อยู่และแผนที่มีหลายช่อง

## โครงสร้างข้อมูลเบื้องต้น

เมื่อเริ่มแต่ละระยะ ให้สร้างหรือแก้ model เฉพาะของระยะนั้น ไม่ทำ migration รวมทุก model ล่วงหน้า:

- `CooperativeCycle`
- `Student`
- `Teacher`
- `Company`

ยังไม่เพิ่มตารางเชื่อมระหว่างนักศึกษากับรอบสหกิจ ตารางคำร้อง หรือข้อมูลเอกสารจนกว่าจะเริ่ม feature นั้น ส่วนชื่อบุคลากรเฉพาะรายการจะเพิ่มพร้อม model รายการฝึกงานหรือเอกสาร เพื่อไม่สร้าง field ที่ยังไม่มีผู้ใช้

## ลำดับดำเนินงาน

### ระยะ A-Completion: รอบสหกิจ

**ขอบเขตที่ agent ทำได้เมื่อได้รับมอบหมาย: แก้เฉพาะ `CooperativeCycle` ที่มีอยู่แล้วเท่านั้น**

1. ตรวจ `git status`, schema, migration และ API/page ที่มีอยู่; ห้ามสร้าง migration หรือ CRUD ซ้ำหากของเดิมครอบคลุมแล้ว
2. ทำ validation ฝั่ง API ให้เป็นกฎในหัวข้อ “กฎข้อมูล” รวมถึง parse วันที่อย่างเข้มงวด และแปลง unique violation เป็น `409`
3. ทำให้ `CLOSED` เป็น terminal ทั้ง `PUT` และ `DELETE`; ก่อนปิดรอบเจ้าหน้าที่เลือกทุกสถานะที่กำหนดได้ทันทีจากฟอร์มเดิม
4. ปรับ UI เฉพาะเท่าที่ต้องใช้สะท้อนกฎ API: แสดงปุ่มจัดการโดยตรง, ไม่เสนอการแก้ไข/ลบรอบปิด, และคงรูปแบบ table, filters, refresh, confirm และ toast ตาม `AGENTS.md`
5. ไม่สร้าง relation, workspace route, application, placement, supervisor, visit, evaluation, budget, document, role/auth หรือ dependency ใหม่
6. ตรวจ flow และ final diff ตามเกณฑ์รับมอบด้านล่าง แล้วรายงานเฉพาะสิ่งที่เปลี่ยนและงานที่จงใจอยู่นอกขอบเขต

#### เกณฑ์รับมอบระยะ A-Completion

- `POST` สร้างรอบด้วยสถานะ `OPEN_FOR_APPLICATION` เสมอ แม้ client ส่งสถานะอื่น
- `POST`/`PUT` ปฏิเสธภาคเรียนที่อยู่นอก `1`–`3`, ปีหรือรุ่นที่ไม่ใช่จำนวนเต็มบวก,
  วันที่ไม่จริง และลำดับวันที่ผิด
- สร้างหรือแก้ไขเป็น `(term, academicYear)` ซ้ำ ได้ `409 Conflict` แม้มี request พร้อมกัน
- เจ้าหน้าที่เปลี่ยนสถานะของรอบที่ยังไม่ปิดได้ทันที; หลังเป็น `CLOSED` ทั้ง `PUT` และ
  `DELETE` ได้ error ที่อธิบายเหตุผล
- ลบรอบที่ยังไม่ปิดและยังไม่มี relation ได้; ยังไม่ต้องทดสอบกรณี relation เพราะยังไม่มี
  model ดังกล่าว
- หน้า `/staff/cooperative-cycles` สะท้อนกฎเดียวกับ API และไม่มี route/เมนู placeholder
  ของ workspace
- รัน `pnpm typecheck`, `pnpm build`, `pnpm exec prisma validate` และ
  `pnpm exec prisma migrate status`; ตรวจ `git diff --check` ก่อนส่งงาน

### ระยะ B: นักศึกษา

**ขอบเขตที่ agent ทำได้เมื่อได้รับมอบหมาย: `Student` เท่านั้น**

1. ขยาย `Student` model และสร้าง migration
2. ทำ API และต่อ `/staff/students` เข้ากับข้อมูลจริง พร้อมตัวกรองรุ่น หมู่ และสถานะ
3. ทำฟอร์ม CRUD นักศึกษา แล้วตรวจรหัสซ้ำ การค้นหา และการเปลี่ยนสถานะใช้งาน

### ระยะ C: อาจารย์

**ขอบเขตที่ agent ทำได้เมื่อได้รับมอบหมาย: `Teacher` เท่านั้น**

1. เพิ่ม `Teacher` model และ migration
2. ทำ API และต่อ `/staff/teachers` เข้ากับข้อมูลจริง โดยใช้รูปแบบ CRUD ที่ผ่านการใช้งานจากนักศึกษา
3. ตรวจรหัสซ้ำ เบอร์มือถือ และสถานะใช้งาน

### ระยะ D: สถานประกอบการ

**ขอบเขตที่ agent ทำได้เมื่อได้รับมอบหมาย: `Company` เท่านั้น ไม่รวมแผนที่**

1. เพิ่ม `Company` model และ migration
2. ทำ API และต่อรายการ/หน้าฟอร์มข้อมูลบริษัท ที่อยู่ และผู้ติดต่อหลักเข้ากับข้อมูลจริง
3. เพิ่มการเลือกจังหวัดและคำนวณภูมิภาคโดยไม่เก็บภูมิภาคซ้ำในฐานข้อมูล
4. ตรวจค้นหา ตัวกรอง และความถูกต้องของที่อยู่

### ระยะ E: แผนที่

1. เลือกผู้ให้บริการจากข้อจำกัดจริงของโครงการ
2. เพิ่มแผนที่ในฟอร์มเดิมโดยไม่เปลี่ยนโครงสร้างที่อยู่
3. ตรวจการค้นหา ปักหมุด แก้ตำแหน่ง และเปิดข้อมูลเดิม

## การตรวจรับแต่ละระยะ

- `pnpm typecheck` ผ่าน
- `pnpm build` ผ่านเมื่อจบหนึ่งโมดูล
- `pnpm exec prisma validate` ผ่านหลังแก้ schema
- migration ใช้กับฐานข้อมูลว่างได้
- ทดสอบ flow เพิ่ม ดู แก้ไข ลบหรือปฏิเสธการลบ และ validation ที่สำคัญ
- UI ใช้งานได้ทั้งจอมือถือและเดสก์ท็อป รวมถึงใช้งานด้วยแป้นพิมพ์
- final diff ไม่มี dependency หรือ abstraction ที่ยังไม่ถูกใช้งาน

## ขั้นถัดไปหลังจบแผนนี้

ออกแบบระบบคำร้องเข้าร่วมรอบสหกิจ ซึ่งจะเป็นจุดเชื่อม `Student`, `CooperativeCycle`, `Company` และข้อมูลชื่อผู้รับเอกสารเฉพาะครั้ง ก่อนต่อยอดไปยังการอนุมัติ การจัดอาจารย์นิเทศ และเอกสารส่งตัว
